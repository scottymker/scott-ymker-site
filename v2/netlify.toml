[build]
  publish   = "public"
  functions = "netlify/functions"

# Pretty /api/* routes to functions
[[redirects]]
  from = "/api/*"
  to   = "/.netlify/functions/:splat"
  status = 200
  force  = true

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["sharp","@netlify/blobs"]
